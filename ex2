#include <stdio.h>

int main() {

    char colors[3][10] = {"grey", "brown", "pink"};
    
    strcpy(colors[0], "red");

    for (int i = 0; i < 3; i++) {
        printf("%s\n", colors[i]);
    }

    return 0;
}
